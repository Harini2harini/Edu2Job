{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py check --deploy && gunicorn edu2job_backend.wsgi:application --bind 0.0.0.0:$PORT --log-level debug",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}
