{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
    // NO buildCommand - Nixpacks will auto-detect
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate && gunicorn edu2job_backend.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120",
    "healthcheckPath": "/health/",
    "healthcheckTimeout": 20
  }
}
